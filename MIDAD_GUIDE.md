# دليل تطوير وصيانة منصة "مداد" 🛠️

هذا الدليل مصمم ليكون مرجعك الأساسي لفهم هيكلية الكود، كيفية التعديل، وإضافة ميزات جديدة دون الوقوع في مشكلة "كود السباغيتي".

---

## 🏗️ هيكلية المشروع (Architecture)

المشروع مبني بنظام **Feature-Based Architecture**:

- `src/lib/`: إعدادات Supabase والاتصالات الأساسية.
- `src/features/`: القلب النابض للمنصة. كل ميزة لها مجلد يحتوي على:
  - `components/`: المكونات الخاصة بالميزة فقط.
  - `services/`: دوال الاتصال بقاعدة البيانات.
- `src/pages/`: الصفحات الكاملة التي تجمع المكونات.
- `src/hooks/`: المنطق البرمجي المشترك (مثلاً `useNotifications`).
- `src/stores/`: إدارة الحالة (State) باستخدام Zustand.

---

## 🔧 كيف أقوم بـ...؟ (How to...)

### 1. إضافة حقل جديد في قاعدة البيانات
إذا أردت إضافة حقل (مثلاً: "عدد القراءات لكل دقيقة"):
1. أضف العمود في جداول Supabase.
2. اذهب إلى ملف الـ Service المناسب وأضف الحقل في استعلامات الـ `select`.
3. استخدم الحقل الجديد في واجهتك.

### 2. تعديل نظام النقاط (Gamification)
النظام الحالي يعتمد على دالة في قاعدة البيانات (RPC).
- لتعديل عدد النقاط: عدل دالة `increment_user_points` في Supabase SQL Editor.
- لإضافة شارة (Badge) جديدة: عدل مكون `UserProfile.jsx`.

### 3. تحسين ميزة سحب يوتيوب
الميزة تعمل حالياً عبر `corsproxy.io` في ملف `PostEditor.jsx`.
- إذا توقف البروكسي: يمكنك تغييره ببروكسي آخر أو استخدام `allorigins.win`.

---

## ⚠️ المشاكل المحتملة وحلولها (Troubleshooting)

| المشكلة | السبب المحتمل | الحل |
|---|---|---|
| **خطأ 404 في Vercel Scripts** | العمل على localhost | تجاهلها، ستختفي عند الرفع الحقيقي. |
| **الصفحة بيضاء تماماً** | خطأ في `MainLayout` أو `App.jsx` | تحقق من كونسول المتصفح؛ غالباً خطأ syntax أو مكتبة مفقودة. |
| **درجة Lighthouse منخفضة** | التشغيل على `npm run dev` | قم دائماً بالفحص على `npm run preview` بعد الـ `build`. |

---

## 🚀 خارطة الطريق المستقبلية (Future Roadmap)

1. **نظام الإشعارات الفوري (Real-time):** تفعيل `supabase.channel` للإشعارات اللحظية.
2. **الوضع الليلي التلقائي:** ربط نظام الألوان بحساسات المتصفح.
3. **تطبيق جوال (PWA):** تحويل الموقع إلى تطبيق قابل للتثبيت.
4. **نظام الدعم المادي:** ربط Stripe أو PayPal لدعم المبدعين.

---
**أنت الآن تملك زمام الأمور في "مداد". انطلق!** 🚀🖋️
